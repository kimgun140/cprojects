/*
TCP_IP프로토콜
APPLICATION
TCP
IP
LINK

LINK 계층
물리적 영역의 표준화 (LAN,WAN,MAN)
IP계층
데이터 전송을 위한 경로의 표준화
TCP/UDP계층
IP계층에서 받은 경로 정보로 실제 송수신 
데이터가 실제로 전달되는 과정에서 어떤 데이터를 언제 어떤상태로(전부 다 받은건지), 순서대로 송수신 되는지 
확인하는 과정을 담당 
송수신 방식의 약속 
APPLICATION 계층 
위에서 말한 과정은 소켓안에서 처리된다. 양식에 맞춰서 처리하면 알아서 된다. 
송수신에 대한 약속들을 프로그래밍할 때 만든다 이게 APPCLICATION 프로토콜이다.



TCP 서버에서의 기보적인 함수 호출 순서


버퍼에 데이터가 들어가서 데이터 송수신이 잘되게 하는거? 퍽유

HALF_CLOSE 스트림 종료? 퍽유 
송수신이 완료가 다된건지 확인? 몰라 ㅅㅂ


SHUT THR FUCK OFF



프로세스의 이해와 활용

프로세스간 통신의 기본이해

*/